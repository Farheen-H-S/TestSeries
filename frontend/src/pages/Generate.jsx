const Generate = () => {
  return (
    <div className="container">
      <h2>Generate Practice Paper</h2>
      <p style={{ marginTop: '1rem', color: 'var(--text-secondary)' }}>
        Select criteria to generate a practice paper.
      </p>
    </div>
  );
};

export default Generate;
